const state = {};
const actions = {
  login(commit, obj) {
    console.log(obj)
  }
};
const mutations = {};
const getters = {};
// import axios from 'axios'

export default {
  namespaced: true,
  actions,
  state,
  getters,
  mutations
}